import { join } from 'path';

export const rootDir = join(__dirname, '..', '..');
export const srcDir = join(rootDir, 'src');

export const RandomLoadingMessage = ['👠 Wait for dommy mommy to finish', 'Hang on, I\'ve been staggered 🥵'];

export const RandomAcivityMessage = ['with Sairina', 'with your heart', ' with explosive Bottlerocket', 'with deez nuts', 'Nare real hard'];